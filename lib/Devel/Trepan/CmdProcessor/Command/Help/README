Files in this directory are help for "help syntax".

A list of sub-names is obtained by Dir.glob('*.pod') in this
directory.

Each file name without the trailing ".pod' is the name of the help
subcategory under "help syntax".

The first line in the file is a summary shown in help summary when
initial an optional initial comment symbol and space is removed.

The file is legitimate POD format, of course.
